package serve

// Static backend is declared in the config schema so configs written
// against the final shape round-trip, but the runtime is not
// implemented in the first release. See SERVE_FEATURE.md §"Initial
// scope" — y-kustomize-local is the only backend wired up. The schema
// stub lives here so future work replaces this single file.
