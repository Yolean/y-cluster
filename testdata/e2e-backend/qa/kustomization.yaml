apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../base
labels:
- pairs:
    env: qa
  includeSelectors: false
patches:
- patch: |-
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: backend-config
    data:
      log-level: debug
